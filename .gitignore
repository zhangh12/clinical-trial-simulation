# Compiled HTML (rendered from .md)
*.html

# Claude local settings
.claude/settings.local.json

# R artifacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# macOS
.DS_Store

# Per-trial simulation output (per-trial folders)
/runs/
/output/

# Stray simulation byproducts at the repo root (skill writes inside
# per-trial folders; anything at root is a test/debug artifact)
/*.rds
/*.png
/*.pdf
